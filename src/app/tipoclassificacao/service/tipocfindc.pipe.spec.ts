import { TipocfindcPipe } from './tipocfindc.pipe';

describe('TipocfindcPipe', () => {
  it('create an instance', () => {
    const pipe = new TipocfindcPipe();
    expect(pipe).toBeTruthy();
  });
});
